# feat: sem6/23cs sub-mapping JSON

Example: Please replace this title, sections in `codeblock` and remove this comment before submitting

## How to test locally 

Steps to reproduce / commands used

- Example: `uv run main.py` and open your browser
- File: `frontend/web/mapping.json`